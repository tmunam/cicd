import React, { Component } from "react";

class AdminHeader extends Component {
    render() {
        return (
            <div>Header react version is: {React.version}</div>
        );
    }
}

export default AdminHeader;
